const babbling = ['aya', 'yee', 'u', 'maa'];

const startChar = ['a', 'y', 'w', 'm'];
const canSay = ['aya', 'ye', 'woo', 'ma'];

babbling.forEach((word) => {
  let answer = word;
  while (answer.length != 0) {}
});
